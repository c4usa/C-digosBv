#include <stdio.h>
#include <string.h>
#include "funciones.h"

struct Estudiante estudiantes[3];
struct Materia materias[3];

int main(int argc, char *argv[])
{
    int opci, cont;
    do
    {
        /* code */
    } while (opci!);
    
    switch (opci)
    {
    case 1:
    crearEstudiante()
        break;
    
    default:
        break;
    }
    return 0;
}